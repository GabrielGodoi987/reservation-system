name: Booking-system-test

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: node:22
    steps:
      - name: Use node
        uses: actions/checkout@v3
      - run: npm ci
      - run: npm run test
      - run: npm run build
